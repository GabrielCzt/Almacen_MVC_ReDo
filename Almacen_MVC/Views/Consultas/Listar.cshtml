@model List<Almacen_MVC.AlmacenMVC.Entities.Producto>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>
@for (int i =0;i<=Model.Count(); ++i)
 {
    <div class="row">
    <div class="col-sm-3 col-md-3">
    @if (i <= Model.Count() - 1)
     {
        <img src="~/Imagenes/@Model[i].ImagenPath" width="100" height="100"/>
        <br/>
        @Model[i].Nombre
        <br />
        <span>
            <b>Presentación: </b>@Model[i].Presentacion
        </span>
        <br />
        <span>
            <b>Costo: </b>@String.Format("{0:c}", Model[i].CostoUnitario)
        </span>
        <br />
        ++i;
    }
    </div>
        <div class="col-sm-3 col-md-3">
            @if (i <= Model.Count() - 1)
                {
                    <img src="~/Imagenes/@Model[i].ImagenPath" width="100" height="100" />
                    <br />
                    @Model[i].Nombre
                <br />
                <span>
                    <b>Presentación: </b>@Model[i].Presentacion
                </span>
                <br />
                <span>
                    <b>Costo: </b>@String.Format("{0:c}", Model[i].CostoUnitario)
                </span>
                <br />
                ++i;
            }
        </div>
        <div class="col-sm-3 col-md-3">
            @if(i<= Model.Count() - 1)
                {
                    <img src="~/Imagenes//@Model[i].ImagenPath" width="100" height="100" />
                    <br />
                    @Model[i].Nombre
                <br />
                <span>
                    <b>Presentación: </b>@Model[i].Presentacion
                </span>
                <br />
                <span>
                    <b>Costo: </b>@String.Format("{0:c}", Model[i].CostoUnitario)
                </span>
                <br />
                }
        </div>
    </div>
    <br />
 }
